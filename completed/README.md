# Matcha build (completed)

These files are for **Matcha only** — not for normal executors.

| File | Use |
|------|-----|
| `AutoDive_Matcha.lua` | VB42 source — run directly in Matcha |
| `AutoDive_Matcha.obf.lua` | VB42 obfuscated loader (`build v3`) |
| `Fisch_Matcha.obf.lua` | Fisch obfuscated loader (`build v3`) |
| `obfuscate.mjs` | Regenerate obf: `node obfuscate.mjs` |

**Executor version:** use `../AutoDive.lua` (Rayfield UI).

**Remote obf (jsDelivr):**
```
https://cdn.jsdelivr.net/gh/typingreflexs/vb42-dive-helper@main/completed/AutoDive_Matcha.obf.lua
https://cdn.jsdelivr.net/gh/typingreflexs/vb42-dive-helper@main/completed/Fisch_Matcha.obf.lua
```
